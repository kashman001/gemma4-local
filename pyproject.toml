[project]
name = "gemma4-local"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mlx>=0.31.2",
    "mlx-lm>=0.31.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
